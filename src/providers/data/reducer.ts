export const DATA_INITIAL_STATE = {
  data: [],
};

// const dataReducer = (state, action) => {
//   switch (action.type) {
//     default:
//       return state;
//   }
// };

// export default dataReducer;
