import { SeriesListContainer } from '../../components';

import './styles.css';

const Series = () => {
  return (
    <section className="limit-width">
      <SeriesListContainer />
    </section>
  );
};

export default Series;
