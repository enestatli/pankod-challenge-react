import { useEffect } from 'react';
import { fetchData } from '../../utils';

interface Props {}

const Home = (props: Props) => {
  return (
    <div className="homepage">
      <h2>Movies Card</h2>

      <h2>Series Card</h2>
    </div>
  );
};

export default Home;
