import { ErrorC } from '../../components';

import './styles.css';

const Error = () => {
  return (
    <section className="error limit-width">
      <ErrorC />
    </section>
  );
};

export default Error;
